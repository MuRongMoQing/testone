# 对仓库员工的每笔库存操作进行独立审批

仓库员工提交的每一笔入库、取出或库内移位都必须附业务单据，并由管理员或经理单独批准或拒绝，而不是只在首次创建货物品种时审批。申请获批前不改变库存余额或账册，获批后默认须在 24 小时内执行，到期未执行则失效并要求重新申请；该流程以数量、价格、货位和凭证的双人复核为优先，接受由此增加的操作步骤。

跨仓调拨只能由发出仓内负责该 SKU 的仓库员工申请，并须获得发出侧和接收侧两个不同人员的审批后才能发运。管理员或经理的调拨需求通过与后续申请关联的通知留痕，不能代替员工申请；管理员暂停及同一管理员覆盖两端时按跨仓双边控制规则由经理替代相应审批侧。
