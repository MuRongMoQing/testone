# 旧用户映射为未分配的新身份

旧 `admin` 与旧 `manager` 迁移为尚未分配品类的新管理员，旧 `viewer` 迁移为尚未分配上级管理员和负责货物的新仓库员工；用户名、Argon2id 密码哈希和历史操作关联保持不变。未完成品类、上级、搭档及货物范围分配前，账户只能完成密码与 TOTP 等账户设置并查看通知，不能访问业务数据。首名新经理和监察员仍通过受控启动配置建立或指定，避免把旧角色名称误当成新治理权限。
