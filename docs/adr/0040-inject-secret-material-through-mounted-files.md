# 通过挂载文件注入部署密钥

TOTP 主加密密钥、TLS 私钥和 HMAC 共享密钥通过可替换的密钥存储端口读取。首版部署适配器从权限受限的挂载文件读取密钥值，环境变量只保存文件路径；密钥不得写入数据库、源码、普通配置文件、日志或审计载荷。

数据库只保存凭据指纹、范围、有效期和撤销事实。适配器必须支持启动时权限检查、内存清理、轮换期间同时读取当前与下一版本以及撤销后停止使用。该选择避免绑定单一云密钥供应商，并为未来替换为专用密钥管理服务保留端口。
